<!--pages/allActivities/index.wxml-->

<view wx:for="{{banner}}" wx:for-item="item">
  <text> \n </text>
  <van-card
    now_person=": {{item.number_now}}"
    need_person=": {{item.number}}  "
    desc="{{item.description}}"
    title="{{item.title}}"
  >
  </van-card>
</view>
